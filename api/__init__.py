from fastapi import APIRouter

router = APIRouter()

"""
Подпакет с роутерами FastAPI:
- shop — пример с магазином;
- roles — пример разграничения прав;
- auth — простая регистрация;
- trips — пример запроса с параметром waybill_id;
- events / volunteers — базовая логика системы «РукаПомощи»
  (мероприятия, записи волонтёров, учёт часов и сертификатов).
"""


